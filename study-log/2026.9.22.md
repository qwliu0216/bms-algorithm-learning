# C 语言基础阶段学习记录

## 已学习内容

我已经基本学习过以下 C 语言知识：

- 数据类型与变量
- `if/else` 条件判断
- `for/while` 循环
- 函数
- 数组
- 字符串
- 指针

目前正在进入结构体，后续学习文件操作和动态内存。

## 本次补充内容

在 `C/00-c-review/` 中整理了一组面向 BMS 数据处理的复习练习：

1. 传感器数据的类型检查和报警判断。
2. 电压采样序列的平均值、最大值、最小值和连续低压统计。
3. 使用函数拆分 BMS 数据分析流程。
4. 使用数组处理采样窗口和突变点。
5. 使用字符串处理电芯标签和状态报告。
6. 使用指针遍历数组、原地限幅，并返回统计结果。

## 复习目标

- 能看懂每个练习的输入、处理和输出。
- 能独立修改阈值并预测结果变化。
- 能为每个程序增加边界条件。
- 能把多个函数拆成 `.h` 和 `.c` 模块，为后续结构体和 BMS 算法做准备。

## 下一步

继续学习结构体，将单个电芯的电压、温度和 SOC 组织成 `Cell`，再将多个 `Cell` 组织成 `BatteryPack`。
