char	1 字节	-128 到 127 或 0 到 255

unsigned char	1 字节	0 到 255

signed char	1 字节	-128 到 127

int	2 或 4 字节	-32,768 到 32,767 或 -2,147,483,648 到 2,147,483,647

unsigned int	2 或 4 字节	0 到 65,535 或 0 到 4,294,967,295

short	2 字节	-32,768 到 32,767

unsigned short	2 字节	0 到 65,535

long	4 字节	-2,147,483,648 到 2,147,483,647

unsigned long	4 字节	0 到 4,294,967,295

float	4 字节	1.2E-38 到 3.4E+38	6 位有效位

double	8 字节	2.3E-308 到 1.7E+308	15 位有效位

long double	16 字节	3.4E-4932 到 1.1E+4932	19 位有效位

void 类型

函数返回为空：void exit (int status);

函数参数为空： int rand(void);

指针指向 void： void \*malloc( size\_t size );

stdbool.h 引入 bool、true、false，这是 C99 才有的，嵌入式/BMS 代码里很常用，比用 int 当布尔值更清晰。

全部加 const（常量）：表示这次采样后这些值不会被修改，编译器可以做优化，也能防止误写

bool 布尔标志位只能是 0（false）或 1（true）



int    i, j, k;可以一次定义多个变量

int \*ptr;ptr 被定义为一个整型指针变量。

常量

const int MAX\_VALUE = 100;

int const MAX\_VALUE = 100;这两种写法在语义上完全等价，编译出来的结果一模一样，MAX\_VALUE 都是"一个不能被修改的 int

